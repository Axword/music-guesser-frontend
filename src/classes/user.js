export default class User {
    constructor (userId, userNick, userEmail, userScore) {
        this.id = userId;
        this.nick = userNick;
        this.email = userEmail;
        this.score = userScore;
    }
}